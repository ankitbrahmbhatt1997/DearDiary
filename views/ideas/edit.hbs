<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Add</title>
    {{> styling}}


</head>

<body>
    {{> main}}
    <div class="container">
        <div class="card-panel">
            <h3>Edit Your Story</h3>
            <form method="post" action="/ideas/{{idea._id}}?_method=PUT">

                <input type="hidden" name="_method" value="PUT">
                <div class="input-field">
                    <label for="title">Title</label>
                    <input type="text" class="form-control" name="title" value="{{idea.title}}" required>
                </div>
                <div class="input-field">
                    <label for="details">Details</label>
                    <textarea class="materialize-textarea" name="details" required>{{idea.details}}</textarea>
                </div>
                <button type="submit" class="btn teal">Submit</button>
            </form>
        </div>
    </div>{{> bootstrap}}
</body>

</html>